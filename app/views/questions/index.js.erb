$questionsDiv = $(`#questions-list`);
$questionsDiv.html(`<%= render 'questions/index', questions: @questions %>`);
removeCard();
$(document).on('ready turbolinks:load', function() {
    editInput();
});
